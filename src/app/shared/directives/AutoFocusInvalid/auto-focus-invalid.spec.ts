import { AutoFocusInvalid } from './auto-focus-invalid';

describe('AutoFocusInvalid', () => {
  it('should create an instance', () => {
    const directive = new AutoFocusInvalid();
    expect(directive).toBeTruthy();
  });
});
