import { AutoFocusInput } from './auto-focus-input';

describe('AutoFocusInput', () => {
  it('should create an instance', () => {
    const directive = new AutoFocusInput();
    expect(directive).toBeTruthy();
  });
});
